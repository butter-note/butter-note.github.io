# 노션 원문 → 버터노트 홈페이지

원문의 문장, 항목 수, 순서, 링크, 체크 상태를 임의로 작성하거나 보충하지 않습니다.
노션에서 지정한 색상·폰트·임의 스타일 대신 홈페이지의 글꼴, 흑백/버터 컬러, 간격을 적용합니다.
페이지 제목과 같은 맨 첫 H1만 본문에서 중복 표시하지 않으며, 나머지 제목은 유지합니다.

## 배포 과정

1. 기존 GitHub Actions에서 노션의 발행 글을 가져옵니다.
2. API 응답은 `content/notion-originals/<page-id>.md`에 수정 없이 보존합니다. 이 폴더는 웹에 공개하거나 커밋하지 않습니다.
3. `content/posts/`에는 이미지 주소를 홈페이지용으로 바꾼 본문과 메타데이터를 생성합니다.
4. `npm run build`의 사전 검사에서 모든 원문과 게시용 본문을 실제 페이지와 동일한 변환기로 렌더링합니다.
5. 알 수 없는 문법, 닫히지 않은 블록, 읽지 못한 표가 있으면 배포를 중단하고 기존 사이트를 유지합니다.
6. 검사 통과 후 정적 HTML을 생성해 GitHub Pages로 배포합니다.

## 대응 방식

| 노션 문법 | 홈페이지 표시 |
| --- | --- |
| 제목 1–4, 문단, 구분선 | 브랜드 글꼴·간격의 제목, 문단, 구분선 |
| 글머리·번호 목록, 할 일, 하위 블록 | 계층/번호/체크 상태를 보존한 읽기 전용 목록 |
| 굵게, 기울임, 취소선, 밑줄, 코드, 링크 | 동일한 의미의 웹 서식. 색상은 사이트 팔레트로 통일 |
| 콜아웃 | 원래 아이콘과 모든 자식 블록을 포함한 버터색 안내 상자 |
| 토글, 토글 제목 | 펼침/접힘이 가능한 안내 영역과 중첩 본문 |
| 여러 열 | 같은 순서의 반응형 열. 작은 화면에서는 세로로 표시 |
| HTML 표 / Markdown 표 | 셀 내용·헤더 역할 유지, 작은 화면에서 가로 스크롤 |
| 이미지 | 이미지와 원본 캡션. 접근 불가 이미지는 안내와 원본 링크 유지 |
| 오디오·영상 | 브라우저 기본 재생 컨트롤과 캡션 |
| 파일·PDF·북마크·임베드 | 캡션과 원본을 여는 링크. 외부 페이지를 임의로 임베드하지 않음 |
| 페이지·DB 참조 | 원본 제목과 링크. 노션의 편집기·실시간 DB 뷰 자체를 복제하지 않음 |
| 동기화 블록 | API가 제공한 자식 본문. 내용 없는 참조는 원본 링크 |
| 멘션·날짜·인용 출처 | 이름/날짜/대상 링크 유지. 권한이 없는 대상은 추가 조회하지 않음 |
| 목차 | 실제 본문 제목과 연결된 링크 |
| 수식 | 안전 모드의 수식 렌더링. 코드/mermaid 블록은 원문 코드로 표시 |
| 빈 블록 | 빈 공간 유지 |
| 사용자 정의 이모지 이름 | API에 이미지가 없는 경우 `:이름:` 원문 유지 |

`format: "notion"`인 메타데이터는 노션의 탭 들여쓰기/블록별 줄을 사용합니다.
기존 일반 Markdown 글은 `format: "markdown"`으로 문단 이어쓰기와 일반 Markdown 규칙을 유지합니다.
노션 API로 가져온 글에는 자동으로 `format: "notion"`이 설정됩니다.
손으로 노션 문법을 붙여 넣는 경우에도 해당 메타데이터를 사용하면 같은 변환기가 적용됩니다.

외부 이미지나 첨부파일의 만료·삭제·접근 권한은 서식 변환으로 복구할 수 없습니다.
현재 이미지 다운로드 실패 안내 정책은 유지하며 노션에서 원본을 다시 첨부해야 합니다.
보안상 스크립트, 임의 CSS, 위험한 주소는 실행하지 않습니다.

규격 기준: https://developers.notion.com/guides/data-apis/enhanced-markdown
검증: `npm test` 및 `npm run content:check`.
